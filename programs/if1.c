#include<stdio.h>
void main()
{
	int x,y;
	printf ("enter the value of 'x' first then 'y'");
	scanf("%d%d",&x,&y);
        printf("start main");

	if(x)
	{
		printf("in first if block");
	}
	if(y)
	{
		printf("in second if block");
	}
	printf("end maikn");
}

